























}    // ...existing code...        );        })            }                return '';                console.error('Error processing album:', error);            } catch (error) {                return createAlbumCard(album, discogsDetails);                                if (!discogsDetails) return '';                                );                    `${album.name} ${album.artist.name || album.artist}`                const discogsDetails = await fetchAlbumDetailsFromDiscogs(            try {        batch.map(async (album) => {    const batchHTML = await Promise.all(        // ...existing code...async function renderAlbums(albums, isSearch = false) {import { createAlbumCard } from './albums.js';